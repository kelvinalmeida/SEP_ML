flask db init
flask db migrate -m "Initial migration"
flask db upgrade



docker-compose down        # Para todos os containers
docker-compose up --build  # Sobe novamente e recompila com a porta certa


docker logs -f <container_name_or_id>



# verificar os logs do código do python (UTILIZANDO DOCKER) sem o buffer atrapalhando...

logging.basicConfig(level=logging.INFO)
logging.info("🔍 dados das respostas no micr. control: %s", data)
sys.stdout.flush()